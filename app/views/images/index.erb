<div class='rowSec'>
  <div class='col-10'>
    <% @result.images.each do |image| %>
      <div class='col-2'>
        <img src="<%= image.values[0] %>" alt="<%= image.keys[0] %>">
      </div>
    <% end %>
  </div>
</div>
